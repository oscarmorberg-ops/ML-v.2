[package]
name = "cybersec-s3-pipeline"
version = "0.1.0"
edition = "2021"
description = "AWS S3 + GuardDuty security scanning pipeline"
authors = ["Oscar Morberg"]
license = "MIT"

[dependencies]
tokio = { version = "1", features = ["full"] }
chrono = "0.4"
