name: CSIO Enterprise CD Pipeline
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Deploy to EKS
      run: |
        helm upgrade --install csio-scanner ./helm-chart \
        --set aws.accountId=695210052267
